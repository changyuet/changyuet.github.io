---
layout: post
title: 杂项
date: 2018-04-15
categories: blog
tags: [Linux]

---

## 杂项

### 安装依赖
```
sudo apt-get install git-core gnupg flex bison gperf build-essential curl g++-multilib lib32z1-dev ccache libgl1-mesa-dev unzip libsdl1.2-dev libesd0-dev libwxgtk3.0-dev squashfs-tools zip libncurses5-dev zlib1g-dev openjdk-8-jre openjdk-8-jdk pngcrush schedtool libxml2 libxml2-utils xsltproc lzop libc6-dev lib32ncurses5-dev gcc-multilib maven tmux screen w3m ncftp liblz4-tool pngquant libssl-dev ccache
```

### dash改为bash

* 终端运行
```
sudo dpkg-reconfigure dash
```
* 然后选择 否 即可 
* 验证查看
```Bash
ls -l /bin/sh
```

### 安装repo
```
mkdir ~/bin
git clone https://gerrit-googlesource.lug.ustc.edu.cn/git-repo.git
cp git-repo/repo ~/bin/
```
### 设置环境
```
sudo gedit /etc/profile
```
 
```
alias repo="~/bin/repo"
export USE_CCACHE=1
export CCACHE_DIR=/media/cqcyu/cache/ccache
```

### 同步源码
```
cd /media/cqcyu/android/
mkdir CM15.1
cd CM15.1
repo init -u https://github.com/LineageOS/android.git -b lineage-15.1
                                or
repo init --repo-url git://github.com/FlymeOS/repo.git -u https://github.com/LineageOS/android.git -b lineage-15.1 --no-repo-verify
repo sync -c -f -j8 --force-sync --no-clone-bundle --no-tags
```
### 编译
```
. build/envsetup.sh
prebuilts/misc/linux-x86/ccache/ccache -M 50G
lunch
make -j8 otapackage
```
* 或者
```
brunch xxx
```

```
mkdir WebApp
cd WebApp
git init
touch README
git add README
git commit -m 'first commit'
git remote add origin git@github.com:changyuet/WebApp.git
git push -u origin master
```
